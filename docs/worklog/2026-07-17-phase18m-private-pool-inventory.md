# Сессия: Фаза 18M, инвентаризация приватных PostgreSQL-границ

- Дата: 2026-07-17
- ID: `2026-07-17-phase18m-private-pool-inventory`
- Линия/фаза: основная линия Velvet Archive, Фаза 18M
- Статус: завершено
- Ветка: `agent/phase18m-private-pool-inventory`
- Базовый commit: `172390deef5ced4fe1527701524b034a8646c87e`

## Перед началом

### Цель

Создать воспроизводимую инвентаризацию оставшихся production-вызовов `_require_pool()` и сформировать следующую очередь Фазы 18 по фактическому коду Velvet Archive.

### Исходный контекст

Фазы 18A–18L перевели четырнадцать repositories, включая полный discussion-контур, на публичный `Database.acquire()`. Текущий план больше не должен полагаться на ручной поиск или старую нумерацию другого проекта.

`Database._require_pool()` остаётся допустимой внутренней деталью самого класса базы, но domain, application, presentation и infrastructure clients не должны обращаться к ней напрямую без явно зафиксированного исключения.

### Планируемый объём

- просканировать весь production package `velvet_bot/`;
- найти вызовы и ссылки `_require_pool` с точными файлами и строками;
- отделить внутреннюю реализацию `Database` от внешних клиентов;
- классифицировать остаток по слоям и доменам;
- добавить самостоятельный скрипт инвентаризации;
- добавить CI/regression-тест, запрещающий новые внешние обращения;
- записать текущий allowlist или фактическую очередь без ложного объявления долга закрытым;
- обновить project memory, development status и changelog;
- определить следующий изолированный repository-срез.

### Критерии готовности

- отчёт строится автоматически из исходников, а не из вручную поддерживаемого списка строк;
- внутреннее определение и вызовы внутри `Database` классифицируются отдельно;
- каждый внешний production-вызов имеет путь, строку, слой и следующий action;
- CI падает при появлении нового неучтённого внешнего обращения;
- аукционная предметная модель не попадает в список задач;
- полный tests workflow и project notes contract проходят;
- Docker workflow запускается только при изменениях runtime/deployment и для этого среза не требуется;
- дневник закрыт фактическим результатом, PR/commit и следующим шагом.

### Риски и ограничения

- текстовый grep может дать ложные совпадения в комментариях и строках, поэтому основной анализ должен использовать AST;
- нельзя автоматически заменять найденные вызовы в этом PR;
- нельзя запрещать внутреннюю реализацию `_require_pool()` внутри `Database` до отдельного решения;
- dynamic access через `getattr` должен быть обнаружен или явно документирован;
- tests, migrations и docs не считаются production clients;
- следующий repository выбирается только после полной инвентаризации.

## После завершения

### Фактически сделано

- добавлен AST-сканер `scripts/inventory_private_pool.py` для всего production package;
- сканер фиксирует путь, строку, столбец, класс, функцию и вид доступа;
- поддержаны прямые attribute-access и динамический `getattr(..., "_require_pool")`;
- внутренние обращения класса `Database` классифицируются отдельно как допустимые;
- добавлены CLI-режимы текста, JSON, полного отчёта, `--fail-on-external` и `--check-baseline`;
- фактическая инвентаризация обнаружила 130 внешних обращений в 35 production-файлах;
- создан `docs/private_pool_inventory.json` с count и SHA-256 identity набора методов каждого файла;
- CI теперь блокирует новый файл, изменение количества вызовов, подмену метода и устаревший baseline после удаления долга;
- создан `docs/private_pool_inventory.md` с категориями и очередью очистки;
- выявлено 53 обращения в legacy query-модулях, 30 в repository-классах внутри крупных модулей, 17 в backup infrastructure, 12 в domain repositories, 7 в handlers, 5 в compatibility-фасадах, 4 в application/service и 2 в infrastructure repository;
- семь handler-вызовов помечены как нарушение слоя: их требуется выносить в use case/repository, а не механически переводить на `acquire()`;
- следующим отдельным срезом выбран `ArchivePreviewRepository` с двумя connection points;
- обновлены `CHANGELOG.md`, `docs/project_memory.md` и `docs/development_status.md`;
- аукционная предметная модель в инвентаризацию и очередь не включена.

### Миграции и совместимость

Миграции, runtime-код, SQL и PostgreSQL-транзакции не изменялись. Добавлены архитектурный сканер, baseline, документация и regression-тесты.

Baseline намеренно фиксирует identity методов без номеров строк: перемещение кода не создаёт шум, а изменение фактического набора обращений требует отдельного review.

### Проверки

- `project notes contract #32` ожидаемо не прошёл из-за стартового статуса worklog `в работе`;
- `tests #548` обнаружил дефект тестового загрузчика Python 3.13; модуль зарегистрирован в `sys.modules` до `exec_module()`;
- `project notes contract #34` — успешно после промежуточной записи;
- `tests #550` выполнил настоящую инвентаризацию и намеренно упал на обнаруженных 130 обращениях, что позволило сформировать baseline;
- `project notes contract #37` — успешно;
- полный workflow `tests #553` с PostgreSQL 16 — успешно после подключения baseline;
- Docker workflow не запускался по path filters: production runtime, dependencies и deployment-файлы не менялись;
- после этой финальной записи tests и notes запускаются повторно на окончательном head.

### PR и commit

- PR: #108 `Фаза 18M: инвентаризировать private pool access`;
- discovery head: `e8296099ddd95c5603f9797b4c585e9d61f53810`;
- первый зелёный baseline head: `7ab5b029cb8d519797ee58d5b123ac509ed47021`;
- финальный squash commit фиксируется GitHub при слиянии PR #108.

### Незавершённое

Обязательных пунктов Фазы 18M не осталось. Зафиксированный долг не считается выполненным: он разбит на последующие отдельные срезы и защищён baseline-контрактом.

### Следующий шаг

Фаза 18N: перевести `ArchivePreviewRepository` на публичный `Database.acquire()`, сохранить load/save SQL и уменьшить baseline с 130 до 128 обращений.