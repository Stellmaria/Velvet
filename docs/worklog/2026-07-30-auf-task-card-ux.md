# 2026-07-30 — карточка личной задачи Ауф

- Дата: 2026-07-30
- ID: `auf-task-card-ux`
- Линия/фаза: Telegram UX / personal task history
- Статус: `завершено`
- Ветка: `agent/auf-task-card-ux`
- Базовый commit: `2c3f451ead55bfd7e01de1af7001fd0fed0bdf76`

## Перед началом

### Цель

Убрать неоднозначный экран, где восемь задач отображаются общим текстовым списком, а все кнопки повторной доставки собраны отдельным блоком ниже и визуально не связаны с конкретными задачами.

### Исходный контекст

Telegram не позволяет прикрепить inline-кнопку к отдельному абзацу одного сообщения. Текущий экран выводит несколько задач в тексте и затем несколько одинаковых кнопок `Доставить`, поэтому пользователь не может уверенно определить, какую задачу откроет каждая кнопка.

### Планируемый объём

- показывать одну задачу за раз;
- выводить модель, статус, тип результата, режим, качество, время и короткий ID;
- заменить техническое `Доставить` на понятное `Получить результат`;
- показывать действие только для успешной задачи;
- добавить навигацию между соседними задачами;
- явно сообщать, что повторная генерация и списание не запускаются;
- убрать формулировку `без операции Ауф`.

### Критерии готовности

- на экране присутствует только одна задача и только одна относящаяся к ней кнопка результата;
- пользователь видит короткий ID и дату задачи;
- кнопка результата не появляется для незавершённых или ошибочных задач;
- переходы `Новее` и `Старее` меняют задачу на одну запись;
- полный CI проходит.

### Риски и ограничения

История остаётся в одном редактируемом Telegram-сообщении. Это намеренно: отдельное сообщение на каждую задачу засоряло бы чат и усложняло обновление статуса.

## После завершения

### Фактически сделано

- история задач переведена на карточку одной записи;
- карточка показывает модель, статус, тип результата, режим, качество, дату и короткий ID;
- image-задачи больше не показывают техническую длительность `6 сек`;
- формулировка `без операции Ауф` заменена на `личная задача / списаний Ауф нет`;
- успешная карточка содержит одну кнопку `📥 Получить результат`;
- для незавершённых, ошибочных и отменённых задач кнопка результата не показывается;
- навигация `Новее` и `Старее` перемещается на одну задачу;
- добавлена кнопка `Обновить карточку`;
- сохранено восстановление provider URL и доставка без нового списания.

### Миграции и совместимость

SQL-миграций нет. Callback `wallet_tasks` сохраняется; меняется только семантика offset с шага страницы на шаг одной задачи. Старые callback-сообщения продолжают открывать историю.

### Проверки

- unit/integration tests;
- type check;
- Docker build;
- project notes contract;
- Telegram navigation inventory.

### Незавершённое

- нет.

### Следующий шаг

После зелёного CI слить PR и обновить Supervisor.

### PR и commit

- PR: `#465`.
- Commit: будет указан после слияния.
