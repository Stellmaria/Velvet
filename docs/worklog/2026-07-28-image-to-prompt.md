# Сессия: image-to-prompt для архивной панели Qwen

- Дата: 2026-07-28
- ID: `2026-07-28-image-to-prompt`
- Линия/фаза: Velvet AI / UX существующей архивной панели Qwen
- Статус: `частично`
- Ветка: `agent/image-to-prompt`
- Базовый commit: `cf252cd9905e8a4a60fd8054c482282fabecce08`

## Перед началом

### Цель

Добавить в существующую owner-панель `Qwen · работа с архивом` операцию,
которая восстанавливает технический промт по одному изображению через уже
настроенную локальную vision-модель.

### Исходный контекст

Панель уже содержит проверку качества, сравнение с референсом, проверку
`промт ↔ результат`, палитру, оформление и историю AI-заданий. Обратная операция
`изображение → промт` отсутствовала. Первоначальная реализация ошибочно
подключала кнопку только к Qwen-панели рабочего пространства, тогда как
фактический owner-сценарий использует системную архивную панель.

### Планируемый объём

- создать клиент image-to-prompt поверх существующих Ollama и
  OpenAI-совместимых vision endpoints;
- добавить компактную кнопку в архивную Qwen-панель;
- принять фото или image-документ через существующий ForceReply-контракт;
- зарегистрировать операцию в постоянной истории AI-заданий;
- вернуть краткий результат в истории и полный текст сообщениями Telegram;
- сохранить локальную AI-блокировку и существующие настройки `AI_VISION_*`;
- добавить regression-проверку callback action и обновить документацию.

### Критерии готовности

- в `Qwen · работа с архивом` отображается кнопка `🪄 Фото → промт`;
- кнопка создаёт форму загрузки изображения;
- результат сохраняется как AI-задание `image_to_prompt`;
- история показывает понятное название и кнопку повторного запуска;
- Python-файлы компилируются, diff проходит whitespace-проверку;
- целевой unit test и живая Windows/Telegram-проверка выполняются до merge.

### Риски и ограничения

Операция зависит от доступности Ollama и фактической поддержки изображений
выбранной моделью. Supervisor обновляет только настроенную стабильную ветку
`main`, поэтому тестовая ветка устанавливается вручную или после merge.

### Обоснование режима стабилизации

1. Улучшается существующий контур Velvet AI и единая архивная Qwen-панель.
2. Владельцу не требуется переносить изображение во внешний инструмент для
   восстановления промта.
3. Предметная область не расширяется: используются существующие изображения,
   локальный Qwen и журнал AI-заданий.
4. Улучшение проверяется callback regression-тестом, компиляцией и живой
   Telegram-проверкой.
5. Сохраняются `AIJobTracker`, локальная AI-блокировка, конфигурация vision и
   существующая presentation/router граница.

## После завершения

### Фактически сделано

- добавлен `ImageToPromptClient` для Ollama и OpenAI-совместимого API;
- в системную панель `Qwen · работа с архивом` добавлена кнопка
  `🪄 Фото → промт`;
- добавлен отдельный canonical Telegram controller для фото/image-документа;
- операция использует `AIJobTracker` и сохраняет полный prompt payload;
- после завершения отправляется отдельное уведомление с количеством готовых
  моделей и ошибок;
- полный результат каждой модели публикуется в чат моноширными HTML-блоками;
- длинный промт автоматически разбивается на части, `.txt` больше не создаётся;
- зарегистрирован новый вид общего AI-задания `image_to_prompt`;
- добавлен последовательный режим сравнения основной и второй vision-модели;
- история AI-заданий получила подпись и повтор операции;
- удалена ошибочная compatibility-интеграция в соседнюю workspace-панель;
- инструкция выровнена по повторяющемуся редакционному формату канала
  `Vᴇʟᴠᴇᴛ Aɴᴀᴛᴏᴍʏ`: `ВАЖНО`, `СТРОГО`, технический блок, суть, композиция,
  лицо и взгляд, руки, тело, волосы, локация, свет, словесная палитра,
  дополнительные теги, расширенный negative и 5–6 HEX-цветов;
- сохранён принцип image-to-prompt: стиль не должен удалять одежду, добавлять
  наготу, придумывать личные референсы, имена, татуировки или навязывать 9:16;
- инструкция требует молча сверять передний/задний план, лево/право, кисти,
  касания, верёвки, взгляд, опоры и перекрытия перед финальным ответом;
- клиент больше не принимает любой текст длиннее 40 символов: проверяются все
  обязательные разделы и минимум пять HEX-цветов в `PALETTE`;
- пустой ответ повторно запрашивается сокращённой инструкцией;
- оборванный ответ автоматически переписывается от последнего неполного раздела,
  максимум двумя recovery-запросами;
- ошибка одной сравнительной модели журналируется как предупреждение и не создаёт
  аварийное уведомление, если другая модель успешно завершилась;
- обновлена документация AI Vision и regression-контракты router/menu/style.

### Миграции и совместимость

Миграции не требуются. Используется существующая таблица AI-заданий и
существующие настройки `AI_VISION_*`. Callback action и новый kind добавлены без
изменения старых действий. Пустой `AI_VISION_COMPARE_MODEL` сохраняет одиночный
режим.

### Проверки

- `python -m py_compile` для изменённых Python-файлов: success;
- `git diff --check`: success;
- добавлены regression-тесты меню, router architecture, регистрации нового kind,
  выбора моделей, редакционного Velvet-контракта, уведомления и разбиения текста;
- добавлен regression-тест восстановления ответа, оборванного внутри `Тело`;
- добавлена проверка, что `PALETTE` с двумя HEX не считается полным результатом;
- отдельный контракт запрещает возврат `answer_document` и `BufferedInputFile`;
- полный suite выполняется GitHub Actions;
- живая Windows/Telegram-проверка выявила оборванный ответ 4B и пустой ответ
  `qwen3-vl:8b`; исправление ожидает повторной проверки на том же изображении.

### PR и commit

Draft PR #331: `Add image-to-prompt flow for local Qwen`.

### Незавершённое

Нужно выполнить целевой/full test suite в `E:\V` и повторно проверить тот же
взрослый парный кадр через Ollama до перевода PR из draft и merge.

### Следующий шаг

Обновить `agent/image-to-prompt` в фактически импортируемой рабочей копии,
перезапустить единственный Supervisor и проверить, что 4B дописывает все разделы,
а отказ 8B отображается как частичный результат без ERROR-тревоги.
