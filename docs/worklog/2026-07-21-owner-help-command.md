# Сессия: полный owner-only справочник slash-команд

- Дата: 2026-07-21
- ID: `2026-07-21-owner-help-command`
- Линия/фаза: Velvet Archive, Telegram owner UX
- Статус: `завершено`
- Ветка: `agent/add-owner-help-command`
- Базовый commit: `bf56339e43edd13bfda4843b8639f91a28210840`

## Перед началом

### Цель

Добавить команду `/help`, доступную только создателю бота, и показать в ней полный актуальный перечень всех зарегистрированных slash-команд Velvet вместе с назначением и совместимыми алиасами.

### Исходный контекст

В BotFather-меню владельца были оставлены только компактные команды `/start`, `/menu` и `/archive`, тогда как код поддерживал около девяноста служебных маршрутов. Существующий integrity-тест учитывал декораторы `Command`, но не обеспечивал отдельный пользовательский справочник и не проверял программные регистрации через `router.message.register`.

### Планируемый объём

- добавить отдельный owner help controller;
- перечислить все команды по функциональным разделам;
- разбить длинный ответ на сообщения, укладывающиеся в лимит Telegram;
- сохранить BotFather-меню компактным для всех ролей;
- не открывать `/help` публичным пользователям и модераторам;
- подключить новый роутер к core operations;
- добавить тест фактического покрытия зарегистрированных команд.

### Критерии готовности

- `/help` обрабатывается ботом и возвращает полный справочник;
- справочник содержит `CommandStart`, декораторные команды и команды из `message.register`;
- каждая страница короче 4096 символов;
- команда доступна владельцу при ручном вызове и отсутствует в публичном и moderator access sets;
- BotFather command menus остаются без расширения;
- тесты проверяют совпадение справочника с реальными маршрутами.

### Риски и ограничения

Доступ опирается на существующий `OwnerAccessMiddleware` и owner allowlist. Справочник остаётся статическим представлением назначения команд, поэтому тест контролирует состав команд, но текстовые описания при изменении поведения всё равно требуют осознанного обновления.

## После завершения

### Фактически сделано

- создан `owner_help.py` с 91 уникальной командой и алиасом, сгруппированными по девяти разделам;
- ответ автоматически разбивается на две страницы;
- `/help` не добавляется ни в одно BotFather command menu и вызывается владельцем вручную;
- owner help router подключён к `core_operations` и внесён в архитектурный инвентарь;
- добавлен AST-тест, который находит `CommandStart`, декораторы `Command` и программные `message.register`;
- тест проверяет access classification, лимит Telegram и присутствие каждой команды в отрендеренном справочнике.

### Миграции и совместимость

SQL-миграции не требуются. Существующие команды, callback contracts и роли не изменены. `/help` использует действующую матрицу доступа: владельцы проходят по allowlist, модераторы и публичные пользователи команду не получают. Компактные command menus остаются прежними.

### Проверки

- синтаксис изменённых Python-файлов проверен через `py_compile`;
- локально подтверждено 91 уникальное имя команды без дублей;
- длины страниц составляют 3432 и 2175 символов;
- после первого CI исправлены compact-menu и architecture inventory contracts;
- GitHub Actions tests, Docker build и project notes contract повторно запускаются на PR #244.

### PR и commit

- PR: #244 `Add owner-only help command`;
- ветка: `agent/add-owner-help-command`;
- код, тесты, архитектурный инвентарь и worklog опубликованы через GitHub Contents API.

### Незавершённое

Функциональных незавершённых пунктов нет. После merge необходимо обновить запущенный экземпляр бота, чтобы новый маршрут `/help` появился в runtime.

### Следующий шаг

Дождаться зелёного CI, слить PR #244 и выполнить штатное обновление через Supervisor, затем вручную вызвать `/help` из аккаунта создателя и проверить две страницы справочника в Telegram.
