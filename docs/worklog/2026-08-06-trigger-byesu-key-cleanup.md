# Trigger Byesu key cleanup

This one-line maintenance marker exists only to create a server-side merge push on `feat/byesu-image-fallback-quality`, allowing the branch-scoped one-shot workflow to run. The workflow removes itself after the validated cleanup commit.
