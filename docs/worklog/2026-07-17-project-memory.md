# Сессия: постоянная память фаз и рабочий журнал

- Дата: 2026-07-17
- ID: `2026-07-17-project-memory`
- Линия/фаза: организационный контур проекта после основной Фазы 20
- Статус: частично
- Ветка: `agent/project-memory-worklog`
- Базовый commit: `d49e69daa07e8654638015fde9944ad7d09003cb`

## Перед началом

### Цель

Закрепить в репозитории историю фаз, фактический статус и обязательную практику заметок до и после каждой работы.

### Исходный контекст

В проекте одновременно существовали несколько независимых нумераций:

- ранний рефакторинг;
- основное развитие Velvet Archive;
- Velvet AI/Qwen.

Из-за повторяющихся названий вроде «Фаза 2» часть контекста оставалась только в чатах и PR. Пользователь потребовал вести записи в самом проекте перед началом и после завершения каждой задачи.

### Планируемый объём

- создать единую карту фаз и открытых обязательств;
- создать каталог рабочих сессий и шаблон записи;
- добавить обязательные инструкции для AI-агентов;
- добавить PR-шаблон;
- добавить автоматическую проверку структуры заметок и обязательного обновления worklog;
- обновить текущий статус разработки ссылками на новый контур памяти.

### Критерии готовности

- основные линии фаз явно разделены;
- известные завершённые и открытые задачи записаны;
- любая будущая задача имеет обязательные блоки `Перед началом` и `После завершения`;
- CI отклоняет содержательный PR без новой записи worklog;
- проверка не требует секретов и не зависит от локальной машины;
- текущая сессия закрыта фактическими проверками и PR/commit.

### Риски и ограничения

- старый план раннего рефакторинга восстановлен частично и должен быть помечен историческим;
- нельзя выдавать первоначальные намерения за фактическое состояние `main`;
- слишком строгая CI-проверка не должна блокировать автоматические release/tag события;
- журнал не должен содержать токены, пароли и строки подключения.

## После завершения

### Фактически сделано

- создан `docs/project_memory.md` с тремя отдельными линиями фаз;
- записаны основные фазы Velvet Archive 1–20;
- отдельно сохранены Velvet AI/Qwen 1–8;
- ранний план рефакторинга помечен историческим и частично восстановленным;
- создан каталог `docs/worklog/` с обязательным шаблоном;
- добавлен `AGENTS.md` с правилами заметок до и после работы;
- добавлен PR-шаблон с планом, итогом, проверками и остатком;
- добавлен `scripts/check_project_notes.py`;
- добавлены unit-тесты контракта заметок;
- добавлен отдельный GitHub Actions workflow;
- актуализирован `docs/development_status.md` до Фазы 20;
- старый `docs/development_phases_analytics.md` помечен историческим и больше не объявлен источником текущего статуса.

### Миграции и совместимость

Миграции базы данных отсутствуют. Runtime-код бота и Supervisor не изменён. Новые правила влияют только на процесс разработки и CI pull request.

### Проверки

- структура итогового diff проверена через GitHub compare;
- автоматические unit tests и `project notes contract` ещё ожидают запуска в PR;
- полный CI проекта ещё ожидает запуска в PR.

### PR и commit

PR ещё не открыт. Текущий head ветки будет зафиксирован после CI.

### Незавершённое

- открыть draft PR;
- получить результаты полного CI;
- при необходимости исправить контракт;
- изменить статус записи на `завершено` и указать PR/commit.

### Следующий шаг

Открыть PR, прогнать новый notes workflow и полный набор существующих проверок.