# Сессия: розничные тарифы и реестр пользователей Ауф

- Дата: 2026-07-30
- ID: 2026-07-30-auf-retail-user-registry
- Линия/фаза: экономика Ауф и пользовательская аналитика
- Статус: частично
- Ветка: feature/auf-retail-user-registry
- Базовый commit: 06640c58d137596c3f2e4b8e15bbd1d4b7fc8241

## Перед началом

### Цель

Разделить реальную себестоимость и пользовательскую цену генераций, скрыть внутренние финансовые данные от обычного пользователя, закрыть системные референсы, добавить уведомления о пополнении, команды начисления и структурный реестр пользователей.

### Исходный контекст

Пользовательский кошелёк показывал внутреннее покрытие API и USD-цены. Розничная цена генерации напрямую вычислялась из себестоимости. Обычный пользователь мог видеть системное пространство в выборе референсов. Заявка на пополнение не создавала отдельного уведомления владельцу, а начисление по Telegram ID или username отсутствовало. Единого реестра первого и последнего входа и обезличенной истории действий не было.

### Планируемый объём

- отдельные розничные тарифы моделей и пакетов;
- скрытие себестоимости и внутренних курсов в обычном интерфейсе;
- исключение системной базы референсов для обычных пользователей;
- уведомление владельца о каждой заявке на пополнение;
- команды `/velvet_grant`, `/velvet_user`, `/velvet_users`;
- таблицы `telegram_users` и `telegram_user_events`;
- middleware без хранения текста сообщений, промтов и файлов.

### Критерии готовности

- пользователь видит только цену в вельветах и рублях;
- списание использует утверждённую розничную сетку, а системная аналитика сохраняет себестоимость;
- системные референсы доступны только глобальному владельцу;
- заявка на пополнение немедленно приходит владельцу с кнопкой подтверждения;
- начисление работает по ID и известному username;
- карточка пользователя показывает входы, активность, генерации, баланс и оплаты;
- миграции, тесты, type check, Docker build и project notes contract проходят.

### Риски и ограничения

Username можно разрешить только после хотя бы одного взаимодействия пользователя с ботом. Реестр намеренно не сохраняет текст сообщений, промты, содержимое файлов и значения callback-параметров. Старые завершённые генерации не пересчитываются по новым тарифам.

## После завершения

### Фактически сделано

- добавлены версионированные розничные тарифы всех текущих фото- и видео-моделей;
- себестоимость провайдера сохранена отдельно от пользовательского списания;
- добавлены фиксированные рублёвые цены пакетов и фиксация цены в счёте;
- внутреннее покрытие API и USD скрыты от обычного пользователя;
- системные референсы исключены из пользовательского выбора и защищены от старых callback;
- владельцу отправляется заявка на пополнение с кнопкой подтверждения;
- пользователю отправляется уведомление после подтверждения оплаты или ручного начисления;
- добавлены команды `/velvet_grant`, `/velvet_user`, `/velvet_users`;
- добавлен структурный реестр пользователей и событий без содержимого сообщений и файлов;
- обновлены тесты платного резервирования под новые тарифы и добавлены новые регрессионные проверки.

### Миграции и совместимость

Добавлены миграции `z025_auf_retail_user_registry.sql` и `z026_auf_package_invoice_pricing.sql`. Старые финансовые операции, счета и завершённые генерации не пересчитываются. Новые версии тарифов применяются только к новым задачам.

### Проверки

Первый `project notes contract` корректно остановил PR из-за статуса `в работе`; статус и итоговые разделы приведены к контракту. Повторный полный CI ожидается для итогового commit.

### PR и commit

- PR: #473
- итоговый функциональный commit перед повторным CI: `3825ea117777572b822161b46556fa9816ceb436`

### Незавершённое

Ожидается повторный CI и возможное обновление generated navigation inventory по фактическому результату генератора.

### Следующий шаг

Устранить найденные CI-ошибки, после полностью зелёных проверок перевести статус в `завершено` и слить PR.
