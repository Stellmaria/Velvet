# Сессия: баланс аккаунта Byesu в служебном экране

- Дата: 2026-08-02
- ID: `byesu-account-balance-20260802`
- Линия/фаза: owner service balances
- Статус: `частично`
- Ветка: `fix/byesu-balance-key-status`
- Базовый commit: `42522af0a19d67333e6a0c423af4d6589b201b44`

## Перед началом

### Цель

Исправить служебный экран балансов так, чтобы он использовал отдельный ключ только для чтения общего баланса аккаунта Byesu и не смешивал этот показатель с квотами отдельных API-ключей.

### Исходный контекст

Production-запрос к `https://byesu.com/dashboard/billing/subscription` возвращал `HTTP 401 Invalid token`. Обработчик скрывал конкретный статус под общей строкой `баланс временно недоступен`, а для авторизации сначала выбирал общий `BYESU_API_KEY`. В кабинете Byesu квоты ключей `Hermes-Codex` и `Hermes-GPT-Pro` показываются отдельно и не являются общим балансом аккаунта.

### Планируемый объём

- добавить отдельную переменную `BYESU_BALANCE_API_KEY`;
- сохранить совместимость с существующим `BYESU_API_KEY` и профильными AI-ключами;
- различать безопасные причины недоступности баланса;
- уточнить подпись показателя в Telegram;
- добавить unit-тесты без реальных сетевых запросов и секретов.

### Критерии готовности

- отдельный ключ баланса имеет приоритет над общим облачным ключом;
- успешный ответ `balance_usd` отображается как долларовый баланс аккаунта;
- HTTP 401 не маскируется под общий временный сбой;
- токены и сырые ответы провайдера не выводятся в Telegram;
- целевые тесты и обязательные CI-проверки проходят.

### Риски и ограничения

- endpoint баланса всё равно требует действующий API-ключ того же аккаунта;
- изменение кода не добавляет секрет на production автоматически;
- квоты отдельных API-ключей не суммируются и не заменяют баланс аккаунта;
- фактический production smoke возможен только после merge, обновления VPS и добавления секретной переменной.

## После завершения

### Фактически сделано

- добавлен приоритетный `BYESU_BALANCE_API_KEY`;
- сохранена обратная совместимость с `BYESU_API_KEY` и профильными AI-ключами;
- HTTP 401, 403 и 429 преобразуются в безопасные понятные статусы без вывода ответа провайдера или секрета;
- сетевые, timeout- и JSON-ошибки различаются;
- подпись в Telegram уточнена до `Byesu · баланс аккаунта`;
- добавлены тесты выбора отдельного ключа, успешного `balance_usd`, HTTP 401 и безопасного сообщения пользователю.

### Миграции и совместимость

SQL-миграций нет. При отсутствии `BYESU_BALANCE_API_KEY` обработчик продолжает использовать прежний порядок совместимости через `BYESU_API_KEY`, затем профильные AI-ключи для `byesu.com`.

### Проверки

- добавлены целевые unit-тесты в `tests/test_auf_menu_provider_balance_hotfix.py`;
- type check прошёл на draft PR;
- полный tests workflow, project notes contract и Docker build повторно запускаются после обновления worklog;
- секреты и значения ключей в Git не добавлены.

### PR и commit

- Draft PR: `#548`;
- ветка: `fix/byesu-balance-key-status`;
- актуальный head после обновления worklog определяется GitHub Actions.

### Незавершённое

- дождаться зелёных обязательных CI-проверок;
- после merge добавить в `/srv/velvet/.env.server` действующий `BYESU_BALANCE_API_KEY`;
- обновить production и проверить кнопку `Обновить балансы`.

### Следующий шаг

После зелёного CI перевести PR из draft, слить после явного разрешения владельца, обновить VPS, добавить секретную переменную и выполнить production smoke без вывода ключа.
