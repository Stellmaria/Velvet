# Сессия: workspace first-run wizard

- Дата: 2026-07-22
- ID: `2026-07-22-workspace-first-run-wizard`
- Линия/фаза: personal workspace product onboarding
- Статус: `завершено`
- Ветка: `agent/workspace-first-run-wizard`
- Базовый commit: `cc0c960b1381a2661bb002da709c1ff946db03e5`

## Перед началом

### Цель

Добавить обязательный мастер первого запуска личного пространства: объяснить владельцу устройство продукта, дать выбрать разрешённые модули, привязать реальные Telegram-чаты и темы, проверить права бота и показать итоговую готовность архива.

### Исходный контекст

Личное пространство уже имело роли, модульные политики, приватность, категории, вселенные, истории и таблицу `workspace_channels`. Однако после создания пользователь получал только сообщение о приватности. Состояние первоначальной настройки не сохранялось, темы Telegram-форума не фиксировались, а владелец не видел единого контрольного списка.

### Планируемый объём

- автоматически запускать мастер после ввода имени нового пространства;
- сохранять прогресс onboarding в PostgreSQL;
- показать интерактивный гид по модулям;
- использовать существующие allowed/enabled module policies;
- сохранять назначения chats/topics с `message_thread_id`;
- проверять, что бот является администратором и может публиковать;
- требовать `can_manage_topics` для веток персонажей в форумных чатах;
- автоматически создавать персональную тему нового персонажа;
- направлять сохранённые материалы в назначенную тему персонажа;
- зеркалировать совместимые назначения в `workspace_channels`;
- добавить команды повторного открытия, статуса, bind и unbind;
- добавить адресную привязку канала по username/chat id из ЛС;
- не включать публичный архив автоматически.

### Критерии готовности

- новый архив сразу открывает мастер вместо тупикового сообщения;
- гид, выбор модулей, назначения и summary доступны кнопками;
- мастер можно закрыть и возобновить без потери прогресса;
- чат и текущая тема сохраняются по реальному Telegram update;
- закрытый или односторонний канал можно привязать из ЛС;
- права бота проверяются до сохранения назначения;
- назначение персонажей принимается только из форумной супергруппы;
- при создании персонажа автоматически создаётся его архивная тема;
- медиа-сервис использует назначенную тему без отдельного ручного шага;
- включённые модули определяют обязательные назначения;
- завершение невозможно при неполной конфигурации;
- один Telegram-чат нельзя присвоить двум пространствам;
- существующий workspace isolation сохраняется;
- полный CI, type check, Docker и project notes contract проходят.

### Риски и ограничения

Telegram не предоставляет безопасный общий список всех чатов пользователя. Поэтому для групп и форумных тем команда привязки выполняется внутри целевого чата/темы. Каналы, где пользователь не может написать команду, подключаются из ЛС по `@username` или `chat_id`; в обоих случаях бот проверяет membership и административные права. Системное пространство Velvet Anatomy не проходит пользовательский onboarding.

## После завершения

### Фактически сделано

- добавлена миграция `910_workspace_first_run_wizard.sql`;
- добавлены `workspace_onboarding` и `workspace_destinations`;
- сохранены status/current step, просмотр гида, подтверждение модулей и завершение;
- добавлен callback-driven мастер с intro, guide, modules, destinations и summary;
- первый ответ на `WorkspaceForm.waiting_workspace_name` перехватывается новым router до legacy handler;
- добавлены `/workspace_setup`, `/workspace_guide`, `/workspace_setup_status`, `/workspace_bind`, `/workspace_unbind` и `/workspace_bind_channel`;
- сохраняются chat id, thread id, chat/topic titles, URL и снимок прав бота;
- форум персонажей требует `is_forum` и право `can_manage_topics`;
- создание персонажа перехватывается перед широким FSM-handler;
- `create_forum_topic` создаёт персональную ветку и `bind_character_archive_topic` связывает её с персонажем;
- существующий media save автоматически копирует новые материалы в эту ветку;
- при ошибке БД созданная тема удаляется, чтобы не оставлять сироту;
- канал из ЛС проверяется через `get_chat` и `get_chat_member`;
- назначения characters/public/publications/discussion/analytics/logs зеркалируются в существующие workspace channel kinds;
- PostgreSQL-триггер запрещает использовать один Telegram-чат в разных пространствах;
- конфликт владения чатом переводится в понятное пользовательское сообщение;
- readiness зависит от реально включённых модулей;
- публичность по-прежнему включается отдельно.

### Миграции и совместимость

Добавлена новая additive migration 910. Существующие таблицы и команды не переименовываются. Существующий `workspace_channels` остаётся источником для уже подключённых analytics/publication/public flows; новый слой хранит более точное назначение и тему форума. Новая форма имени зарегистрирована раньше legacy router, а автоматическое создание темы зарегистрировано раньше широкого character FSM-handler.

### Проверки

Добавлены `tests/test_workspace_onboarding.py`, `tests/test_workspace_onboarding_channel_bind.py` и `tests/test_workspace_character_topic_provisioning.py` с проверками обязательных назначений, readiness, схемы миграции, порядка router, команд, thread persistence, проверки каналов, запрета cross-workspace chat reuse, создания темы и удаления orphan topic. Обновлены справочник команд и машинные inventories. Полный тестовый workflow, type check, Docker build, backup restore drill и project notes contract выполняются GitHub Actions.

### PR и commit

Изменения подготовлены в ветке `agent/workspace-first-run-wizard` и опубликованы в PR #290. Финальный merge commit фиксируется после зелёного CI.

### Незавершённое

После merge требуется живая проверка в Telegram: создать тестовое личное пространство, подключить форум персонажей, тему материалов и канал публикаций, создать персонажа и убедиться, что его персональная ветка появляется автоматически. Старые личные пространства получат status `not_started` и смогут открыть мастер вручную.

### Следующий шаг

После эксплуатационной проверки подключить `workspace_destinations.media` и `workspace_destinations.references` к отдельным автоматическим потокам, если общая staging-тема действительно нужна помимо персональных веток и workspace isolation. Затем добавить переименование Telegram-темы при переименовании персонажа и безопасное закрытие темы при удалении профиля.
