name: phase14 alias confirmation fix

on:
  push:
    branches:
      - agent/phase14-analytics-management-split

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/phase14-analytics-management-split
          fetch-depth: 0

      - name: Apply alias confirmation fix
        run: python scripts/_phase14_fix2.py

      - name: Commit fixed source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Исправить подтверждение удаления алиаса"
          git push origin HEAD:agent/phase14-analytics-management-split
