name: patch ai job tracking

on:
  push:
    branches:
      - agent/fix-ai-quality-operations-menu
    paths:
      - scripts/_patch_ai_job_tracking.py
      - .github/workflows/patch-ai-job-tracking.yml
  pull_request:
    branches:
      - main
    paths:
      - scripts/_patch_ai_job_tracking.py
      - .github/workflows/patch-ai-job-tracking.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/fix-ai-quality-operations-menu
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Apply patch
        run: |
          set -o pipefail
          python - <<'PY'
          from pathlib import Path
          path = Path("scripts/_patch_ai_job_tracking.py")
          source = path.read_text(encoding="utf-8")
          source = source.replace("    old = '''", "    old = r'''", 3)
          source = source.replace(
              "        return\n    await status.edit_text(",
              "        return\n\n    await status.edit_text(",
              1,
          )
          path.write_text(source, encoding="utf-8")
          PY
          python scripts/_patch_ai_job_tracking.py 2>&1 | tee patch-ai-job-tracking.log

      - name: Upload patch diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: patch-ai-job-tracking-log
          path: patch-ai-job-tracking.log

      - name: Validate syntax
        run: python -m compileall -q velvet_bot

      - name: Commit patched handlers
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add velvet_bot/handlers/velvet_ai.py
          git add velvet_bot/handlers/velvet_ai_visual.py
          git add velvet_bot/handlers/velvet_ai_formatting.py
          if git diff --cached --quiet; then
            echo "No handler changes to commit"
          else
            git commit -m "Отслеживать интерактивные AI-задания"
            git push origin HEAD:agent/fix-ai-quality-operations-menu
          fi
