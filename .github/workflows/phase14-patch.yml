name: phase14 analytics management split

on:
  push:
    branches:
      - agent/phase14-analytics-management-split

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/phase14-analytics-management-split
          fetch-depth: 0

      - name: Apply exact analytics split
        run: python scripts/_phase14_patch.py

      - name: Commit patched sources
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Фаза 14: разделить analytics management"
          git push origin HEAD:agent/phase14-analytics-management-split
